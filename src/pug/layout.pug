block variables
  - let pageTitle = 'MISHKA';

doctype html
html(lang="ru")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title #{pageTitle}
  body
    block modals
    .intro
      include ./header.pug
      block hero
    main
      block main
    include ./footer.pug
